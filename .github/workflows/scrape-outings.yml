name: Scrape Outings (Agenda)

on:
  schedule:
    # Tous les jours à 3h UTC
    - cron: "0 3 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  scrape-outings:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Postgres du VPS filtré par IP : accès par tunnel SSH (clé limitée au transfert)
      - name: Tunnel SSH vers le Postgres du VPS
        uses: ./.github/actions/vps-db-tunnel
        with:
          ssh-host: ${{ secrets.VPS_TUNNEL_HOST }}
          ssh-user: ${{ secrets.VPS_TUNNEL_USER }}
          ssh-key: ${{ secrets.VPS_TUNNEL_KEY }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 lxml psycopg2-binary python-dotenv curl_cffi

      - name: Scrape agendas (M+ Info, m2A le mag, JDS.fr)
        env:
          # Écriture directe dans la base Supabase principale (tables Outing, OutingCategory, OutingTag).
          DATABASE_URL: ${{ secrets.SITE_DATABASE_URL || secrets.DATABASE_URL }}
          ASSOCIATION_ID: ${{ secrets.ASSOCIATION_ID }}
        run: |
          python scripts/scrape_outings.py --source all --past-days=7 --ahead-days=120

      - name: Invalider le cache sorties
        if: success()
        run: |
          curl -sf "https://www.mulhouse68.fr/api/cron/revalidate?tag=outings&key=${{ secrets.CRON_SECRET }}" \
            && echo "Cache sorties invalidé" \
            || echo "Revalidation cache ignorée"

      - name: Checkout MulhouseGPT (deploy key)
        if: success()
        uses: actions/checkout@v4
        with:
          repository: Jef-Infojef/MulhouseGPT
          ssh-key: ${{ secrets.MULHOUSEGPT_DEPLOY_KEY }}
          path: mulhousegpt

      # Postgres du VPS filtré par IP : accès par tunnel SSH (clé limitée au transfert)
      - name: Tunnel SSH vers le Postgres du VPS
        uses: ./.github/actions/vps-db-tunnel
        with:
          ssh-host: ${{ secrets.VPS_TUNNEL_HOST }}
          ssh-user: ${{ secrets.VPS_TUNNEL_USER }}
          ssh-key: ${{ secrets.VPS_TUNNEL_KEY }}

      - name: Setup Node.js
        if: success()
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: mulhousegpt/package-lock.json

      - name: Install MulhouseGPT dependencies
        if: success()
        working-directory: mulhousegpt
        run: npm ci

      - name: Sync sorties à venir (RAG)
        if: success()
        working-directory: mulhousegpt
        env:
          JINA_KEY_ENV: github
          PUPPETEER_SKIP_DOWNLOAD: "true"
          SITE_DATABASE_URL: ${{ secrets.SITE_DATABASE_URL || secrets.DATABASE_URL }}
          RAG_DATABASE_URL: ${{ secrets.RAG_DATABASE_URL }}
          JINA_API_KEY: ${{ secrets.JINA_API_KEY }}
        run: npx tsx scripts/outings-sync.ts

      - name: Log failure to DB
        if: failure()
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          python scripts/log_github_failure.py