name: Daily Weather Backfill

on:
  schedule:
    # Runs at 23:00 UTC (00:00 CET) so data is ready right at midnight
    - cron: '0 23 * * *'
  workflow_dispatch:

jobs:
  backfill:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Postgres du VPS filtré par IP : accès par tunnel SSH (clé limitée au transfert)
      - name: Tunnel SSH vers le Postgres du VPS
        uses: ./.github/actions/vps-db-tunnel
        with:
          ssh-host: ${{ secrets.VPS_TUNNEL_HOST }}
          ssh-user: ${{ secrets.VPS_TUNNEL_USER }}
          ssh-key: ${{ secrets.VPS_TUNNEL_KEY }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

      - name: Run weather backfill script
        run: npx tsx scripts/weather-backfill.ts
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          WEATHER_LOCATION: Mulhouse

      - name: Alerter sur Telegram si echec
        if: failure()
        uses: ./.github/actions/telegram
        with:
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          chat_id: ${{ secrets.TELEGRAM_CHAT_ID }}
          status: echec
          title: ${{ github.workflow }} — echec